{
  "name": "Telegram Notification",
  "description": "Send recording info, cover image and video to Telegram via MTProto (supports files over 50 MB and HTTP/SOCKS5 proxies). Use Username for public groups/channels, Chat ID for private ones",
  "params": {
    "api_id": { "label": "API ID (from my.telegram.org)" },
    "api_hash": { "label": "API Hash" },
    "bot_token": { "label": "Bot Token (from @BotFather)" },
    "username": { "label": "Public group/channel username (without @, for public targets, e.g. mygroupname)" },
    "chat_id": { "label": "Chat ID (for private groups/channels, format: -100xxxxxxxxxx)" },
    "proxy": { "label": "Proxy (http:// or socks5://)" },
    "send_video": { "label": "Also send video file" }
  }
}
